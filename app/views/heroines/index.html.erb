<h1>All Heroines</h1>


<!-- <%= form_tag("powers_path", method: "get") do %>
  <%= label_tag(:power_id, "Search for:") %>
  <%= text_field_tag(:power_name) %>
  <%= submit_tag("Search") %>
<% end %> -->


<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name.titlecase, heroine_path(heroine) %>
    </li>
    <br>
  <% end %>
</ul>
